{
  "name": "aur3m-api",
  "version": "0.1.0",
  "private": true,
  "description": "Azure Functions middle layer for AUR3M.COM",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "clean": "powershell -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\"",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests configured yet\""
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "mssql": "^12.2.1"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.9",
    "@types/node": "^22.13.14",
    "typescript": "^5.8.2"
  }
}
